package devices;

public class LightSensor extends Sensors{
    public LightSensor(String name, String type, String subType,String actualStatus){
        super(name, type, subType, actualStatus);
    }
}
